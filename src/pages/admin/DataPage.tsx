
import React from "react";
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function DataPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Admin - Data & Analytics</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Dashboard for charts, data, and analytics. (You can add charts here later!)</p>
      </CardContent>
    </Card>
  );
}
